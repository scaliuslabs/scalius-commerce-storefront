# .env.example
# Scalius Commerce Storefront - Environment Variables
# Copy this file to `.env` for local development.
# In production, secrets should be set via `npx wrangler pages secret put <NAME>`

# ── Required Secrets (Do not share these) ───────────────────────────────────

# Tokens for internal API communication and cache purging.
# These MUST exactly match the tokens set in your backend.
API_TOKEN="your_backend_api_token_here"
JWT_SECRET="your_backend_jwt_secret_here"
PURGE_TOKEN="your_backend_purge_token_here"

# ── Required Variables (Configure in wrangler.jsonc or .env) ────────────────

# Full API V1 Path (The backend URL where your Hono API runs)
PUBLIC_API_URL="http://localhost:4321/api/v1"

# Base API Path (Used for image optimization and some external auth redirects)
PUBLIC_API_BASE_URL="http://localhost:4321"

# ── Optional Variables ──────────────────────────────────────────────────────

# The URL where your storefront is hosted
STOREFRONT_URL="http://localhost:4322"

# CDN Domain — Used to whitelist images from your R2 storage
CDN_DOMAIN_URL="cdn.yourdomain.com"
